# Core ML stack — CPU build (arm64 / Apple Silicon dev machine).
# Python 3.12 (system 3.14 has no torch wheels yet).
torch==2.12.*
torch_geometric>=2.6,<3.0
numpy>=1.26,<3.0
scikit-learn>=1.4
networkx>=3.2

# ONNX export (dynamo/AOT exporter needs onnxscript) + parity check
onnx>=1.16
onnxscript>=0.1
onnxruntime>=1.18
